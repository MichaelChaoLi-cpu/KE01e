#!/usr/bin/env python3
"""Preprocessing script for data/raw/reused_local/KE01b/road_sections/kumamoto_road_sections_preprocessed.parquet
Output: data/processed/road_sections_preprocessed.parquet

Generated by data-preprocessing skill.
"""
from __future__ import annotations

import pandas as pd
from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]  # project root

src = ROOT / "data/raw/reused_local/KE01b/road_sections/kumamoto_road_sections_preprocessed.parquet"
dst = ROOT / "data/processed/road_sections_preprocessed.parquet"

# --- Load ---
df = pd.read_parquet(src)

# --- Select and rename columns ---
rename_map = {
    "Road Section ID": "Road Section ID",
    "Section From Node ID": "Section From Node ID",
    "Section To Node ID": "Section To Node ID",
    "Network Component ID": "Network Component ID",
    "Road Section Length (m)": "Road Section Length (m)",
    "Road Edge Count": "Road Edge Count",
    "Baseline Section Travel Time (min)": "Baseline Section Travel Time (min)",
    "Vertical Level": "Vertical Level",
    "Emergency Route Membership": "Emergency Route Membership",
    "Route ID": "Route ID",
    "Route Name": "Route Name",
    "Road Category": "Road Category",
    "Road State": "Road State",
    "Width Category": "Width Category",
    "Toll Category": "Toll Category",
    "Hazard Exposure Class": "Hazard Exposure Class",
    "Assumed Speed (km/h)": "Assumed Speed (km/h)",
    "Road Available": "Road Available",
    "Network Analysis Eligible": "Network Analysis Eligible",
    "Geometry": "Geometry",
}
df = df[list(rename_map.keys())].rename(columns=rename_map)

# --- Save ---
dst.parent.mkdir(parents=True, exist_ok=True)
df.to_parquet(dst, index=False, engine="pyarrow")
print(f"Saved {len(df):,} rows × {len(df.columns)} cols → {dst.relative_to(ROOT)}")
